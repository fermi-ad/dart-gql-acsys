name: dart_gql_acsys
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  ferry: ^0.16.0+1
  gql_http_link: ^1.2.0
  gql_websocket_link: ^2.0.1
  gql_code_builder: ^0.13.1
  web_socket_channel: ^2.4.5
  built_collection: ^5.1.1
  built_value: ^8.9.5
  collection: ^1.19.1 # Use the latest version
  pure_dart_ui: ^0.1.10

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.3
  ferry_generator: ^0.12.0
  build_runner: ^2.4.13

